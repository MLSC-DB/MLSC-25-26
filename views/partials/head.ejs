<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>MLSC DB</title>

<!-- Tailwind config MUST be defined before the CDN script -->
<script>
  window.tailwind = window.tailwind || {};
  window.tailwind.config = {
    theme: { extend: {} },
    safelist: [
      // Background + responsive bg size used on <body>
      "bg-[url(/images/bg.jpg)]",
      "bg-[auto_650px]",
      "sm:bg-[auto_1300px]",

      // Popup window container (created dynamically)
      "fixed",
      "bg-slate-600",
      "border-2",
      "border-slate-100",
      "transition-all",
      "duration-300",
      "scale-95",
      "opacity-0",
      "w-[300px]",
      "h-[400px]",
      "sm:w-[600px]",
      "sm:h-[400px]",

      // Popup header + controls
      "flex",
      "justify-between",
      "items-center",
      "px-1",
      "py-1",
      "bg-slate-100",
      "cursor-move",
      "select-none",
      "gap-1",
      "w-6",
      "h-6",
      "bg-slate-400",
      "hover:bg-slate-700",
      "text-sm",
      "font-bold",
      "border",

      // Popup content area
      "w-full",
      "h-[calc(100%-2.5rem)]",
      "overflow-auto",
      "text-white",
      "p-2",

      // Taskbar button created at runtime
      "items-center",
      "gap-2",
      "text-black",
      "px-3",
      "py-1",
      "font-mono",
      "border",
      "border-[#808080]",
      "bg-gradient-to-b",
      "from-[#d4d0c8]",
      "to-[#b5b5b5]",
      "shadow-inner",
      "hover:brightness-90",
      "brightness-90",
      "active:translate-y-[1px]",
      "translate-y-[1px]",
      "h-10",
      "rounded-sm",
      "w-10",
      "h-10",

      // Common toggled states via JS
      "hidden",
      "opacity-100",
      "scale-100",
      "scale-0",
      "pointer-events-none",

      // Misc content placeholders
      "text-center",
      "mt-16",
    ],
  };
</script>
<!-- Tailwind CSS (pinned version) -->
<script src="https://cdn.tailwindcss.com/3.4.17"></script>

<link rel="icon" type="image/x-icon" href="/favicon.ico" />

<!-- Preloader Style & Base Styles -->
<style>
  @keyframes move-xp {
    0% {
      transform: translateX(-100%);
    }
    100% {
      transform: translateX(600%);
    }
  }

  .loader-track {
    animation: move-xp 3s linear infinite;
  }

  .bar-rect {
    width: 16px;
    height: 100%;
    background-color: skyblue;
    margin-right: 2px;
  }

  .xp-btn {
    font-family: Tahoma, sans-serif;
    font-size: 12px;
    font-weight: bold;
    padding: 2px 8px;
    color: black;
    background: linear-gradient(to bottom, #dbeeff, #7fbfff);
    border: 1px solid #3a6ea5;
    border-radius: 4px;
    box-shadow: inset 0 0 0 1px #ffffffa1, inset 0 1px 0 0 #ffffffa1;
    white-space: nowrap;
    margin-right: 2px;
    transition: background 0.2s ease-in-out;
  }

  .xp-btn:hover {
    background: linear-gradient(to bottom, #c5e3ff, #66b2ff);
  }

  .xp-btn:active,
  .xp-btn.active {
    background: linear-gradient(to bottom, #7fbfff, #dbeeff);
    box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.4);
  }

  .xp-taskbar-btn {
    font-family: Tahoma, sans-serif;
    box-shadow: inset 1px 1px white, inset -1px -1px #888;
  }

  .taskbar-btn {
    border-width: 1px;
    border-color: #808080;
    background: linear-gradient(to bottom, #d4d0c8, #b5b5b5);
    box-shadow: inset 1px 1px #fff, inset -1px -1px #808080;
  }

  .taskbar-btn:hover {
    box-shadow: 0 0 4px rgba(0, 0, 0, 0.5);
  }

  .taskbar-btn.active-task {
    background: linear-gradient(to bottom, #c0c0c0, #a0a0a0);
    box-shadow: inset 1px 1px #ffffff, inset -1px -1px #808080;
  }

  .image-render-pixelated {
    image-rendering: pixelated;
  }

  html,
  body.lock-scroll {
    overflow: clip !important;
    height: 100vh !important;
  }

  html,
  body {
    max-height: 100vh;
    overflow-x: hidden;
  }
</style>

<!-- (Debug helper and fallback removed for production cleanliness) -->
